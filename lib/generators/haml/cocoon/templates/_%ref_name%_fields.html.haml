%section.row.nested-fields[f.object]
  = f.inputs do
    = link_to_remove_association '✖', f,
          class: 'close', title: 'Destroy <%= ref_name.titleize %>',
          data: {confirm: 'Are you sure you want to destroy this entity and all its children.'}
    %br
<% for attribute in ref_attributes -%>
    = f.input :<%= attribute %>
<% end %>
    %hr
