%div.nested-group{id: nested.pluralize.dasherize}
  - unless f.blank?
    .panel
      .panel-heading
        - if has_one
          %b= nested.titleize
        - else
          %b= nested.pluralize.titleize
      - if has_one
        = render "view_#{nested}_fields", item: f
      - else
        - f.each do |item|
          = render "view_#{nested}_fields", item: item
        %hr
