.nested-fields{class: 'row'}
  = f.inputs do
    = link_to_remove_association "&times;".html_safe, f, class: 'close', title: 'Destroy <%= ref_name.titleize %>', data: { confirm: 'Are you sure you want to destroy this entity and all its refren.' }
    %br
<% for attribute in ref_attributes -%>
    = f.input :<%= attribute %>
<% end %>
    %hr
